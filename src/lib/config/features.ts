/**
 * Application Feature Flags
 * 
 * Set a feature to `true` to show it in the UI, or `false` to hide it.
 * This is used for "Coming Soon" features or features in maintenance.
 */
export const FEATURES = {
  metaAds: false, // Hide Meta Ads integration card from Settings
};
